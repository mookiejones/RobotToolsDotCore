<UserControl x:Class="RobotTools.View.OptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RobotTools.View"
             mc:Ignorable="d"
             xmlns:editor="clr-namespace:RobotTools.UI.Editor;assembly=RobotTools.UI"
             d:DesignHeight="450" d:DesignWidth="800"
              d:DataContext="{d:DesignInstance Type=editor:EditorOptions,IsDesignTimeCreatable=True}"
             >

    <ListBox>
      <CheckBox Content="HighlightCurrentLine" IsChecked="{Binding HighlightCurrentLine,Mode=TwoWay}" />
    <CheckBox Content="Show Line Numbers" IsChecked="{Binding ShowLineNumbers,Mode=TwoWay}" />
    <CheckBox Content="Allow Scrolling Below Document" IsChecked="{Binding AllowScrollingBelowDocument,Mode=TwoWay}"/>

  </ListBox>
   

</UserControl>
