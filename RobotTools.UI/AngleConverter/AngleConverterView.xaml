<UserControl x:Class="RobotTools.UI.AngleConverter.AngleConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RobotTools.UI.AngleConverter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition/>
      <RowDefinition/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <local:ValueBox DataContext="{Binding InputItems}" Header="Input" Grid.Row="0"/>
    <local:ValueBox DataContext="{Binding InputItems}" Header="Output" Grid.Row="1"/>

    <TextBox x:Name="txtMatrix" IsReadOnly="True" TextAlignment="Center" Text="{Binding Matrix}" Height="80"  SpellCheck.IsEnabled="False" />

    StackPanel>
  </Grid>
</UserControl>
